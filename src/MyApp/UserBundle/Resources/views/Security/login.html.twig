{% trans_default_domain 'FOSUserBundle' %}
{% extends "MyAppUserBundle:Security:Frontlog.html.twig" %}

{% block formLog %}

    <form action="{{ path("fos_user_security_check") }}" method="post">
        {% if csrf_token %}
            <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
        {% endif %}
        <div class="inner-holder">
            <label>Nom d'utilisateur</label>
            <input type="text" autofocus  placeholder="Nom d'utilisateur"
                   id="username" name="_username" value="{{ last_username }}" required="required" />

        </div>

        <div class="inner-holder">
            <label>Mot de passe</label>

            <input type="password"  placeholder="Mot de passe"
                   id="password" name="_password" required="required" />
        </div>
        <br>

        <div class="inner-holder">

        Se souvenir de moi <span></span>
            <input type="checkbox" id="remember_me" name="_remember_me" value="on" />

        <br>
        </div>



        <div class="inner-holder">
            <div class="login-social-link centered">



                <button class="submit"
                        type="submit" id="_submit" name="_submit" value="{{ 'security.login.submit'|trans }}">
                    Se Connecter</button>
                <br>
                <br>
                {% block soc %}
                    {% for owner in hwi_oauth_resource_owners() %}
                        {% if owner =='facebook' %}
                            <a class="btn btn-facebook -{{ owner }} " href="{{ hwi_oauth_login_url(owner) }}">
                                <i class="fa fa-facebook -{{ owner }}" href="{{ hwi_oauth_login_url(owner) }}">
                                    {{ owner | trans({}, 'HWIOAuthBundle')|capitalize }}</i>
                            </a>

                        {% endif %}
                    {% endfor %}
                {% endblock soc %}

                <hr>
            </div>


        </div>
            <div class="inner-holder">
                 <span class="pull-right">
                <a data-toggle="modal" href="{{ path('fos_user_resetting_request') }}"> mot de passe oublié?</a>
            </span>

                {% if error %}
                    <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                {% endif %}

        </div>
       <hr>


        <div class="registration">
            Vous n'avez pas encore un compte?<br/>
            <a class="" href="{{ path('fos_user_registration_register') }}">
                Créer un compte
            </a>
        </div>
        </div>

        </div>

    </form>




<!--

    <form class="form-login" action="{{ path("fos_user_security_check") }}" method="post" class="form-signin">
        <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
        <div class="login-container">
            <h2 class="form-login-heading">Se Connecter maintenant</h2>
            <br>

           <form>
                <div class="login-wrap">
                <input type="text" autofocus class="form-control" placeholder="Nom d'utilisateur"
                       id="username" name="_username" value="{{ last_username }}" required="required" /> <br>



                <input type="password" class="form-control" placeholder="Mot de passe"
                       id="password" name="_password" required="required" /> <br>


                    <input type="checkbox" id="remember_me" name="_remember_me" value="on" />
                    <label for="remember_me">{{ 'security.login.remember_me'|trans }}</label>

                    <label class="checkbox">
                                <span class="pull-right">
                                    <a data-toggle="modal" href="{{ path('fos_user_resetting_request') }}"> mot de passe oublié?</a>

                                </span>
                    </label>


                    <button class="btn btn-theme btn-block" href="index.html" type="submit" id="_submit" name="_submit" value="Se connecter"><i class="fa fa-lock"></i>
                        Se Connecter</button>

                    {% if error %}
                        <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                    {% endif %}
                    <hr>
                    <div class="login-social-link centered">
                        <p>Se connecter avec les réseaux sociaux </p>

                        {{ facebookButton({'locale':'fr','url': 'https://web.facebook.com/Nada.tc'}) }}

                        {% block hwi_oauth_content %}
                            {% for owner in hwi_oauth_resource_owners() %}
                                {% if owner =='facebook' %}
                                    <a class="btn btn-facebook -{{ owner }} " href="{{ hwi_oauth_login_url(owner) }}">
                                    <i class="fa fa-facebook -{{ owner }}" href="{{ hwi_oauth_login_url(owner) }}">   {{ owner | trans({}, 'HWIOAuthBundle')|capitalize }}</i>
                                    </a>

                                {% endif %}
                            {% endfor %}
                        {% endblock hwi_oauth_content %}
                        <button class="btn btn-twitter" type="submit"><i class="fa fa-twitter"></i> Twitter</button>
                    </div>
                    <div class="registration">
                        Vous n'avez pas encore un compte?<br/>
                        <a class="" href="{{ path('fos_user_registration_register') }}">
                            Créer un compte
                        </a>
                    </div>
                </div>

            </form>






        </div>
    </form> -->




{% endblock formLog %}